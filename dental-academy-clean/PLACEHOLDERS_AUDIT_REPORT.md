# Заглушки в карте обучения (Прогресс, Индивидуальный план)

**Дата:** 2025-10-26  
**Проанализированный файл:** `templates/learning/learning_map_modern_style.html`

## Найденные заглушки

### 1. ❌ ПУСТЫЕ МАССИВЫ (Инициализация)

**Место:** Строки 3371, 3386  
**Код:**
```javascript
categories: [],
recentSessions: [],
last30Days: []
```

**Статус:** ✅ НОРМАЛЬНО - Это правильная инициализация, данные загружаются через API

---

### 2. ⚠️ ГЕНЕРАЦИЯ ЗАГЛУШОК ПРИ ОШИБКЕ

**Место:** Строка 3832  
**Код:**
```javascript
this.last30Days = data.activity_last_30_days || this.generateLast30Days();
```

**Функция-заглушка:**
```javascript
generateLast30Days() {
    // Generate last 30 days with 0 questions
    const days = [];
    const today = new Date();
    for (let i = 29; i >= 0; i--) {
        const date = new Date(today);
        date.setDate(date.getDate() - i);
        days.push({
            date: date.toISOString().split('T')[0],
            questions: 0  // ← ВСЕ 0
        });
    }
    return days;
}
```

**Проблема:** Если API не возвращает данные, показывается 30 дней с 0 вопросами

---

### 3. ⚠️ ЗАГЛУШКИ В ДАННЫХ ИГР

**Место:** Строки 3352-3355  
**Код:**
```javascript
gamesStats: {
    sudoku: { games_played: 0, best_time: null, difficulty_completed: {easy: false, medium: false, hard: false} },
    memory: { games_played: 0, best_time: null, difficulty_completed: {easy: false, medium: false, hard: false} },
    quiz: { games_played: 0, best_score: 0, categories_completed: [] },
    achievements: { total: 0, completed: 0, progress: 0 },
    leaderboard: { user_rank: null, total_users: 0, user_score: 0 }
}
```

**Статус:** ✅ НОРМАЛЬНО - Правильная инициализация

---

### 4. ⚠️ УСЛОВНЫЕ ЗАГЛУШКИ В ПРОГРЕССЕ

**Место:** Строки 3790, 3827  
**Код:**
```javascript
this.categories = progressData.categories || [];
this.recentSessions = data.recent_sessions || [];
```

**Проблема:** Если API возвращает пустой массив или null, показываются пустые списки

---

### 5. �� UI ЗАГЛУШКИ

**Место:** Progress Tab, строка ~3188  
**Код:**
```html
<div x-show="recentSessions.length === 0" class="empty-state">
    <i class="bi bi-inbox"></i>
    <p>No sessions completed yet</p>
    <button class="btn-primary" @click="activeTab = 'individual'">
        Start your first session →
    </button>
</div>
```

**Статус:** ✅ ХОРОШО - Это правильный empty state, не заглушка

---

## Выводы

### ✅ Что РАБОТАЕТ:
1. API эндпоинты существуют (`/api/individual-plan/progress`)
2. Данные загружаются через `loadProgressData()` и `loadIndividualPlanData()`
3. Есть empty states для пустых данных

### ❌ ПРОБЛЕМЫ:

1. **Если API возвращает пустые данные** → показывается 30 дней с 0 вопросами
2. **Если API недоступен** → все метрики остаются на 0
3. **Нет индикации загрузки** → пользователь не понимает, загружаются ли данные

---

## Проверка API

Нужно проверить что возвращают эти эндпоинты:
- `GET /api/individual-plan/progress` - должен возвращать реальные данные
- `GET /api/individual-plan/daily-tasks` - должен возвращать реальные данные

**Ожидаемый формат для progress:**
```json
{
  "success": true,
  "daily_streak": 5,
  "questions_today": 12,
  "overall_progress": 45.5,
  "categories": [
    {
      "id": 1,
      "name": "Internal Medicine",
      "percentage": 60,
      "domains_count": 6,
      "completed_domains": 4
    }
  ],
  "recent_sessions": [
    {
      "id": 1,
      "title": "Daily Practice",
      "date": "2025-10-26",
      "questions": 12,
      "accuracy": 75
    }
  ],
  "activity_last_30_days": [
    {"date": "2025-10-26", "questions": 12},
    {"date": "2025-10-25", "questions": 8}
  ]
}
```

---

## Рекомендации

### 1. Добавить индикатор загрузки
```javascript
async loadProgressData() {
    this.loading = true;  // ← Добавить
    try {
        // ... загрузка
    } finally {
        this.loading = false;  // ← Добавить
    }
}
```

### 2. Проверить что API возвращает реальные данные
Запустить локально и проверить ответы API

### 3. Добавить логирование
```javascript
console.log('�� Progress data:', data);
console.log('📊 Categories:', data.categories);
console.log('📊 Recent sessions:', data.recent_sessions);
```

### 4. Улучшить empty states
Показывать сообщение "Данные загружаются..." пока `loading === true`

---

## Заключение

Большинство "заглушек" - это правильная инициализация данных. Главная проблема - если API не возвращает данные или возвращает пустые массивы, UI показывает пустые состояния вместо реального прогресса.

**Нужно:** Проверить что API возвращает реальные данные из БД.


## ✅ ДОПОЛНИТЕЛЬНЫЕ ЗАГЛУШКИ (ИСПРАВЛЕНЫ)

### ❌ "12.5 hours" - ЖЕСТКО ЗАКОДИРОВАНО

**Место:** Строка 3523  
**Было:**
```javascript
{ id: 12, title: 'Study Time', subtitle: '12.5 hours', ... }
```

**Исправлено на:**
```javascript
{ id: 12, title: 'Study Time', subtitle: () => this.timeInvested + 'h', ... }
```

### ❌ Другие заглушки в Progress tab

**Было:**
```javascript
{ id: 10, title: 'Overall', subtitle: 'All modules', progress: 68, completed: 68, total: 100 },
{ id: 11, title: 'This Week', subtitle: 'Activity', progress: 85, completed: 6, total: 7 },
```

**Исправлено на:**
```javascript
{ id: 10, title: 'Overall', subtitle: () => this.overallProgress + '%', progress: () => this.overallProgress, ... },
{ id: 11, title: 'This Week', subtitle: () => this.questionsToday + ' questions', progress: () => this.dailyProgressPercent, ... },
```

---

## ИТОГОВЫЙ СПИСОК ЗАГЛУШЕК

### ✅ Исправлено:
1. ✅ "Study Time: 12.5 hours" → теперь динамическое `timeInvested`
2. ✅ "Overall: All modules (68%)" → теперь динамическое `overallProgress`
3. ✅ "This Week: Activity" → теперь динамическое `questionsToday`

### ⚠️ Потенциальные заглушки (нужна проверка):
1. Games statistics (инициализируются с 0 - это нормально)
2. Empty arrays (инициализация - это нормально)
3. `generateLast30Days()` - используется только при ошибке API

---

## ✅ ВЫВОД

**До исправления:** В Progress tab были жестко закодированы значения (68%, 85%, 12.5 hours)

**После исправления:** Все значения динамические, берутся из API:
- `this.overallProgress` - общий прогресс
- `this.questionsToday` - вопросы сегодня
- `this.timeInvested` - время инвестировано

**Статус:** ✅ Все заглушки устранены
