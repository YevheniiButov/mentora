# ✅ Проблема решена: psutil

## Проблема
```
WARNING:app:Could not import routes: No module named 'psutil'
INFO:app:Creating minimal routes...
ERROR:app:   Available lang routes: []
```

## Причина
- `psutil` был установлен в системном Python, но **НЕ** в `venv`
- Сервер запускался через `venv`, поэтому не мог найти `psutil`
- Из-за этого импорт маршрутов падал с ошибкой
- Приложение переходило в режим "minimal routes" и **НЕ регистрировало `main_bp`**
- Поэтому маршруты `/en/`, `/uk/` и т.д. не работали (404)

## Решение
✅ `psutil` установлен в `venv`:
```bash
pip install psutil
```

## Что делать дальше

1. **Остановите текущий сервер** (Ctrl+C в терминале, где он запущен)

2. **Перезапустите сервер:**
   ```bash
   PORT=5002 python3 run.py
   ```

3. **Проверьте логи** - должно быть:
   ```
   ✅ Все Flask extensions initialized successfully
   ✅ Statistics API blueprint registered successfully
   ✅ All route blueprints registered successfully
   ```
   **БЕЗ** `WARNING:app:Could not import routes`

4. **Откройте** `http://127.0.0.1:5002/en/`

5. **Проверьте логи** - должно быть:
   ```
   INFO:app:🌐 BEFORE_REQUEST: path=/en/
   INFO:app:🔍 route_by_domain: path=/en/
   INFO:app:🔍 before_request_main: path=/en/
   ```
   И **НЕ должно быть** 404!

## Что такое psutil?

`psutil` (process and system utilities) - это библиотека Python для получения информации о:
- Процессах системы
- Использовании CPU
- Использовании памяти
- Сетевых соединениях

В этом проекте используется в некоторых роутах для мониторинга системы.

## Проверка установки

```bash
# В venv
python3 -c "import psutil; print('✅ psutil доступен')"
```

Если ошибка - установите:
```bash
pip install psutil
```


