services:
  - type: web
    name: mentora
    env: python
    plan: free
    buildCommand: |
  echo "🚨 CRITICAL UPDATE: Принудительное обновление production - $(date)" &&
  pip install --no-cache-dir -r requirements.txt &&
  echo "Installing dependencies completed" &&
  flask db upgrade &&
  echo "Database tables created" &&
  flask seed-database &&
  echo "Data seeding completed" &&
  flask check-production-data &&
  echo "Production data check completed" &&
  flask force-load-data &&
  echo "Force data loading completed" &&
  echo "✅ CRITICAL UPDATE: Production обновлен - $(date)"
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.18
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: mentora-db
          property: connectionString
      - key: DIGID_MOCK_MODE
        value: "true"
      - key: DIGID_ENABLED
        value: "true"
      - key: FLASK_DEBUG
        value: "false"
      - key: MAIL_SUPPRESS_SEND
        value: "false"
      - key: MAIL_SERVER
        value: "smtp-relay.brevo.com"
      - key: MAIL_PORT
        value: "587"
      - key: MAIL_USE_TLS
        value: "true"
      - key: MAIL_USE_SSL
        value: "false"
      - key: MAIL_USERNAME
        value: "96d92f001@smtp-brevo.com"
      - key: MAIL_PASSWORD
        value: "JrbVFGpHhgynKMOQ"
      - key: MAIL_DEFAULT_SENDER
        value: "Mentora <noreply@mentora.com>"
      - key: EMAIL_CONFIRMATION_SALT
        value: "mentora-production-salt-2025" 