# 📊 ОТЧЕТ О ФУНКЦИОНАЛЕ КАРТЫ ОБУЧЕНИЯ (LEARNING MAP)

**Дата анализа:** 2025-11-12  
**Версия:** 1.0  
**Проект:** MENTORA - Dental Academy Clean

---

## 🎯 ОБЩЕЕ ОПИСАНИЕ

**Карта обучения (Learning Map)** — это центральный хаб платформы MENTORA, который объединяет все обучающие модули в единый интерфейс. Это главная точка входа для пользователей после регистрации и диагностики.

**URL:** `/<lang>/learning-map` (например, `/en/learning-map`, `/ru/learning-map`)  
**Blueprint:** `learning_map_bp`  
**Основной шаблон:** `templates/learning/learning_map_modern_style.html`  
**Фреймворк:** Flask (backend) + Alpine.js (frontend) + Bootstrap 5 (UI)

---

## 📑 СТРУКТУРА: 7 ОСНОВНЫХ ВКЛАДОК

### 1. **INDIVIDUAL (Индивидуальный план)** 🎯
**Порядок:** Первая вкладка (по умолчанию)  
**Иконка:** Человек (bi-person)  
**Цвет:** Фиолетовый (#8B5CF6)

#### Функционал:
- **Ежедневный план обучения** — автоматически генерируется на основе диагностики
- **Задачи на сегодня:**
  - Тесты (Tests) — диагностические сессии
  - Термины (Terms) — изучение медицинских терминов
  - Виртуальные пациенты (Virtual Patients) — клинические сценарии
  - Английские тексты (English Passages) — чтение и понимание
- **Прогресс в реальном времени:**
  - Процент выполнения задач
  - Статусы: "Klaar" (Готово), "Bezig" (В процессе)
  - Прогресс-бары для каждой задачи
- **Таймер до сброса** — обратный отсчет до полуночи (reset задач)
- **Статистика дня:**
  - Очки за сегодня (`todayScore`)
  - Время обучения (`studyTimeToday`)
  - Процент выполнения (`getTodayCompletionPercentage()`)
- **Кнопки действий:**
  - "Start" / "Doorgaan" (Продолжить) для незавершенных задач
  - Повтор задач после завершения всех
- **API endpoints:**
  - `GET /api/daily-tasks` — получение задач на сегодня
  - `GET /api/today-progress` — получение прогресса за сегодня
  - `POST /api/complete-task` — отметка задачи как выполненной

#### Модели данных:
- `PersonalLearningPlan` — персональный план обучения
- `StudySession` — сессии обучения
- `DiagnosticSession` — диагностические сессии
- `UserTermProgress` — прогресс по терминам
- `VirtualPatientAttempt` — попытки виртуальных пациентов
- `UserEnglishProgress` — прогресс по английским текстам

---

### 2. **IRT (Item Response Theory)** 🧪
**Порядок:** Вторая вкладка  
**Иконка:** Микросхема (bi-cpu)  
**Цвет:** Синий (#3B82F6)  
**Статус:** BETA

#### Функционал:
- **Три типа тестов:**
  1. **Quick Test (30 вопросов)** — быстрая проверка знаний
     - Результаты показываются в конце
     - Адаптивный выбор вопросов (IRT)
  2. **Full Test (60 вопросов)** — полная оценка
     - Покрывает все домены
     - Комплексная диагностика способностей
  3. **Learning Mode (30 вопросов)** — обучающий режим
     - Объяснения после каждого вопроса
     - Мгновенная обратная связь
     - Не влияет на диагностику
- **Адаптивная система:**
  - IRT (Item Response Theory) для оценки способностей
  - Динамический выбор вопросов по сложности
  - Оценка theta (θ) — уровень способности пользователя
- **API endpoints:**
  - `POST /diagnostic/start` — запуск диагностической сессии
  - `GET /diagnostic/session/<id>` — получение сессии
  - `POST /diagnostic/response` — отправка ответа
  - `GET /diagnostic/results/<id>` — получение результатов

#### Модели данных:
- `DiagnosticSession` — сессии диагностики
  - `session_type`: `quick_test`, `full_test`, `learning_mode`
  - `current_ability` (theta) — текущая оценка способности
  - `ability_se` — стандартная ошибка оценки
- `DiagnosticResponse` — ответы пользователя
  - `selected_answer` — выбранный ответ
  - `is_correct` — правильность
  - `ability_before` / `ability_after` — изменение способности
- `Question` — вопросы с IRT параметрами
- `IRTParameters` — параметры IRT (a, b, c)

---

### 3. **PREMIUM (Премиум доступ)** ⭐
**Порядок:** Третья вкладка  
**Иконка:** Звезда (bi-star-fill)  
**Цвет:** Оранжевый (#F59E0B)  
**Доступ:** Только для премиум пользователей (`user.membership_type == 'premium'`)

#### Функционал:
- **Безлимитный доступ** ко всем обучающим модулям:
  1. **Tests (Тесты)** — неограниченный доступ ко всем вопросам
  2. **Terms (Термины)** — изучение всех медицинских терминов
  3. **English Reading (Английский)** — все тексты для чтения
  4. **Virtual Patients (ВП)** — все сценарии виртуальных пациентов
- **Метрики:**
  - Отображаются компактные блоки с иконками
  - Клик по блоку запускает соответствующий модуль
- **API endpoints:**
  - Проверка премиум статуса через `user.is_premium_active`
  - Те же endpoints, что и для обычных модулей, но без ограничений

#### Модели данных:
- `User.membership_type` — тип подписки (`free`, `premium`)
- `User.membership_expires` — дата истечения подписки

---

### 4. **GAMES (Игры)** 🎮
**Порядок:** Четвертая вкладка  
**Иконка:** Геймпад (bi-controller)  
**Цвет:** Розовый (#EC4899)

#### Функционал:
- **Игровые модули:**
  1. **Sudoku** — логическая игра для тренировки мышления
  2. **Medical Memory** — запоминание медицинской терминологии
  3. **Quick Quiz** — быстрая проверка знаний
- **Баннер "Super Mentora":**
  - Платформер в стиле Mario & Contra
  - Ссылка на `/games/dentist-dash`
  - Изображение: `/static/images/superment.png`
- **Достижения и лидерборд:**
  - Система достижений
  - Соревновательный элемент
- **API endpoints:**
  - `GET /games/list` — список доступных игр
  - `POST /games/score` — сохранение результатов

#### Модели данных:
- `UserAchievement` — достижения пользователя
- `UserActivity` — активность пользователя
- `UserStreak` — серии дней обучения

---

### 5. **PROGRESS (Прогресс)** 📈
**Порядок:** Пятая вкладка  
**Иконка:** График (bi-graph-up)  
**Цвет:** Голубой (#06B6D4)

#### Функционал:
- **Общая статистика:**
  - Прогресс по категориям (Tests, Terms, English, Virtual Patients)
  - Процент выполнения по каждой категории
  - Количество завершенных элементов
- **Детальная статистика по категориям:**
  - **Tests:**
    - Всего завершено
    - Завершено сегодня
    - Средний балл
    - Всего вопросов
  - **Terms:**
    - Всего изучено
    - Изучено сегодня
    - Освоено (mastered)
    - Всего доступно
  - **English Reading:**
    - Завершено текстов
    - Завершено сегодня
    - Средний балл
    - Всего доступно
  - **Virtual Patients:**
    - Завершено сценариев
    - Завершено сегодня
    - Средний балл
    - Всего доступно
- **Активность обучения:**
  - Heatmap активности за последние 30 дней
  - Визуализация дней с активностью
  - Легенда: от "Minder" (Меньше) до "More" (Больше)
- **Последние сессии:**
  - Список недавних сессий обучения
  - Дата, количество вопросов, точность
  - Бейджи успеха (зеленый ≥80%, желтый <80%)
- **Достижения (Achievements):**
  - **Streak badges:**
    - 7 дней подряд (flame animation)
    - 30 дней подряд (fist animation)
  - **Questions badges:**
    - 100 вопросов (arm animation)
    - 500 вопросов (brain animation)
  - Lottie анимации для визуализации
  - Блокировка/разблокировка наград
- **API endpoints:**
  - `GET /api/user/stats` — общая статистика пользователя
  - `GET /api/progress/categories` — прогресс по категориям
  - `GET /api/progress/activity` — активность за 30 дней
  - `GET /api/progress/sessions` — последние сессии
  - `GET /api/achievements` — достижения пользователя

#### Модели данных:
- `UserProgress` — общий прогресс пользователя
- `UserAchievement` — достижения
- `UserStreak` — серии дней
- `DiagnosticSession` — для статистики тестов
- `UserTermProgress` — для статистики терминов
- `UserEnglishProgress` — для статистики английского
- `VirtualPatientAttempt` — для статистики ВП

---

### 6. **PLANNER (Планировщик)** 📅
**Порядок:** Шестая вкладка  
**Иконка:** Календарь (bi-calendar-check-fill)  
**Цвет:** Зеленый (#10B981)

#### Функционал:
- **Календарь обучения на 28 дней:**
  - Сетка 4x7 (4 недели по 7 дней)
  - Визуализация дней обучения
  - Выделение сегодняшнего дня
  - Блокировка дней 15-28 до прохождения BIG Test на день 14
- **Информация о цикле:**
  - Текущий цикл (cycle number)
  - День в цикле (day_in_cycle / 28)
  - Множитель сложности (difficulty multiplier)
- **План на выбранный день:**
  - Отображается в правой колонке при выборе дня
  - Список задач на день
  - Прогресс по каждой задаче
  - BIG Test на день 14 (специальный блок)
- **BIG Test:**
  - Комплексный тест на 14-й день
  - 60 вопросов из последних 2 недель обучения
  - Разблокирует дни 15-28 после прохождения
- **API endpoints:**
  - `GET /api/planner/calendar` — получение календаря на 28 дней
  - `GET /api/planner/day/<day>` — план на конкретный день
  - `POST /api/planner/complete-day` — отметка дня как завершенного
  - `GET /api/planner/cycle-info` — информация о текущем цикле

#### Модели данных:
- `PersonalLearningPlan` — персональный план
  - `weekly_schedule` — недельное расписание (JSON)
  - `total_weeks` — общее количество недель
  - `daily_goal_minutes` — цель на день (минуты)
- `StudySession` — сессии обучения
  - `study_day` — день обучения (1-28)
  - `session_type` — тип сессии
  - `planned_duration` — запланированная длительность
- `DiagnosticSession` — для BIG Test на день 14

---

### 7. **ARCHIVE (Архив)** 📦
**Порядок:** Седьмая вкладка  
**Иконка:** Архив (bi-archive-fill)  
**Цвет:** Серый (#64748B)

#### Функционал:
- **Просмотр завершенных активностей:**
  1. **English Passages (Английские тексты)**
  2. **Dutch Terms (Голландские термины)**
  3. **Medical Tests (Медицинские тесты)**
  4. **Virtual Patients (Виртуальные пациенты)**
- **Фильтрация и поиск:**
  - Поиск по названию/тексту
  - Сортировка: по дате, по баллу, по времени
  - Порядок: по возрастанию/убыванию
- **Пагинация:**
  - 20 элементов на страницу
  - Навигация: предыдущая/следующая страница
  - Информация: "Страница X из Y"
- **Статистика в правой колонке:**
  - Общее количество по категориям
  - Средний балл/уровень мастерства
  - Иконки и цветовое кодирование
- **Карточки элементов:**
  - Название/заголовок
  - Бейдж с баллом (высокий/средний/низкий)
  - Метаданные: дата, время, количество вопросов
  - Категория
  - Стрелка для перехода к деталям
- **API endpoints:**
  - `GET /api/archive/english` — архив английских текстов
  - `GET /api/archive/terms` — архив терминов
  - `GET /api/archive/tests` — архив тестов
  - `GET /api/archive/virtual-patients` — архив ВП
  - `GET /api/archive/stats` — статистика архива

#### Модели данных:
- `UserEnglishProgress` — прогресс по английским текстам
- `UserTermProgress` — прогресс по терминам
- `DiagnosticSession` — диагностические сессии (тесты)
- `VirtualPatientAttempt` — попытки виртуальных пациентов
- `EnglishPassage` — тексты для чтения
- `MedicalTerm` — медицинские термины
- `VirtualPatientScenario` — сценарии виртуальных пациентов

---

## 🏗️ АРХИТЕКТУРА

### Backend (Flask)

#### Основной файл:
- **`routes/learning_map_routes.py`** — все маршруты карты обучения
  - `@learning_map_bp.route("")` — главный маршрут
  - `@learning_map_bp.route("/api/tour/status")` — статус тура
  - `before_request_learning_map()` — обработчик перед запросами

#### Дополнительные API:
- **`routes/archive_api.py`** — API для архива
- **`routes/diagnostic_routes.py`** — API для диагностики (IRT)
- **`routes/english_routes.py`** — API для английских текстов
- **`routes/virtual_patient_routes.py`** — API для виртуальных пациентов
- **`routes/games_routes.py`** — API для игр
- **`routes/learning_planner_routes.py`** — API для планировщика
- **`routes/user_statistics_api.py`** — API для статистики

### Frontend (Alpine.js)

#### Основной компонент:
```javascript
function learningMapApp() {
    // Управление вкладками
    activeTab: 'individual' | 'irt' | 'premium' | 'games' | 'progress' | 'planner' | 'archive'
    
    // Состояние тура
    tourActive: boolean
    tourStep: number
    dontShowTourAgain: boolean
    
    // Данные
    dailyTasksData: object
    progressData: object
    plannerSelectedDayPlan: object
    
    // Методы
    selectTab(tab)
    startTask(type)
    loadDailyTasks()
    loadProgress()
    startTour()
    skipTour()
    persistTourState(completed)
}
```

#### Дополнительные компоненты:
- **`archiveComponent()`** — управление архивом
  - `activeCategory`, `items`, `pagination`, `stats`
  - `loadItems()`, `changePage()`, `viewItem()`
- **`plannerCalendar()`** — календарь планировщика
  - `studyDays`, `selectedDay`, `currentCycleInfo`
  - `selectDay()`, `loadCalendar()`

### Модели данных (SQLAlchemy)

#### Основные модели:
1. **`User`** — пользователь
   - `learning_map_tour_completed` — завершен ли тур
   - `membership_type` — тип подписки
   - `requires_diagnostic` — требуется ли диагностика

2. **`PersonalLearningPlan`** — персональный план
   - `weekly_schedule` — расписание (JSON)
   - `total_weeks` — количество недель
   - `daily_goal_minutes` — цель на день

3. **`DiagnosticSession`** — диагностическая сессия
   - `session_type` — тип сессии
   - `current_ability` — оценка способности (theta)
   - `status` — статус (in_progress, completed)

4. **`UserProgress`** — прогресс пользователя
   - Общий прогресс по всем модулям

5. **`UserTermProgress`** — прогресс по терминам
   - `mastery_level` — уровень мастерства
   - `next_review` — следующее повторение

6. **`UserEnglishProgress`** — прогресс по английскому
   - `score`, `total_questions` — результаты

7. **`VirtualPatientAttempt`** — попытка ВП
   - `completed` — завершена ли
   - `percentage_score` — процент баллов

8. **`StudySession`** — сессия обучения
   - `study_day` — день обучения (1-28)
   - `session_type` — тип сессии

---

## 🎨 UI/UX ОСОБЕННОСТИ

### Дизайн:
- **Современный glassmorphism** — полупрозрачные карточки с размытием
- **Градиентные фоны** — анимированные морфирующие формы
- **Адаптивная верстка** — Bootstrap 5 + кастомные стили
- **Иконки Bootstrap Icons** — единый стиль иконок
- **Lottie анимации** — для достижений и визуальных эффектов

### Интерактивность:
- **Alpine.js** — реактивность без тяжелых фреймворков
- **Живые обновления** — прогресс обновляется в реальном времени
- **Плавные переходы** — CSS transitions для всех элементов
- **Hover эффекты** — интерактивные карточки и кнопки

### Тур по интерфейсу:
- **Интерактивный гид** — пошаговое знакомство с картой
- **Чекбокс "Не показывать снова"** — сохранение предпочтений
- **Сохранение в БД** — `learning_map_tour_completed` в таблице `user`
- **Параметр `?reset_tour=1`** — для тестирования (сброс тура)

---

## 🔄 ИНТЕГРАЦИИ

### Внутренние модули:
1. **Диагностика (IRT)** — `routes/diagnostic_routes.py`
2. **Английские тексты** — `routes/english_routes.py`
3. **Виртуальные пациенты** — `routes/virtual_patient_routes.py`
4. **Игры** — `routes/games_routes.py`
5. **Термины** — `routes/flashcard_routes.py`
6. **Планировщик** — `routes/learning_planner_routes.py`
7. **Статистика** — `routes/user_statistics_api.py`

### Внешние сервисы:
- **Resend API** — для email уведомлений (не используется напрямую в карте)
- **Lottie Files** — для анимаций достижений

---

## 📊 СТАТИСТИКА И АНАЛИТИКА

### Отслеживаемые метрики:
1. **Прогресс по категориям:**
   - Tests, Terms, English, Virtual Patients
   - Процент выполнения, средние баллы
2. **Активность:**
   - Heatmap за 30 дней
   - Количество вопросов в день
3. **Достижения:**
   - Streak (серии дней)
   - Количество вопросов
4. **Ежедневные задачи:**
   - Процент выполнения
   - Время обучения
   - Очки за день

### Кэширование:
- `clear_user_stats_cache(user_id)` — очистка кэша статистики
- Кэш очищается при каждом запросе к карте обучения

---

## 🌍 МУЛЬТИЯЗЫЧНОСТЬ

### Поддерживаемые языки:
- `en` (English)
- `ru` (Русский)
- `uk` (Українська)
- `nl` (Nederlands)
- `es` (Español)
- `pt` (Português)
- `tr` (Türkçe)
- `fa` (فارسی)
- `ar` (العربية)

### Переводы:
- Все тексты через функцию `t('key', lang)`
- Файлы переводов: `translations/*.py`
- Ключи переводов для карты: `learning_map`, `individual_plan`, `irt`, `premium`, `games`, `progress`, `planner`, `archive`, и т.д.

---

## 🔐 БЕЗОПАСНОСТЬ И ДОСТУП

### Аутентификация:
- `@login_required` — все маршруты требуют авторизации
- Проверка профиля: `check_profile_complete()` — перед доступом к карте

### Права доступа:
- **Premium вкладка** — только для `user.membership_type == 'premium'`
- **Остальные вкладки** — доступны всем авторизованным пользователям

### Валидация:
- Проверка языка из URL
- Валидация параметров запросов
- Обработка ошибок с fallback значениями

---

## 🚀 ПРОИЗВОДИТЕЛЬНОСТЬ

### Оптимизации:
- **Пагинация** — для архивов (20 элементов на страницу)
- **Ленивая загрузка** — данные загружаются по требованию
- **Кэширование статистики** — для быстрого доступа
- **Минификация CSS/JS** — через CDN (Bootstrap, Alpine.js)

### Загрузка данных:
- **Асинхронные запросы** — через Fetch API
- **Индикаторы загрузки** — спиннеры и overlay
- **Обработка ошибок** — graceful degradation

---

## 📝 ЗАМЕТКИ ДЛЯ РАЗРАБОТЧИКОВ

### Важные файлы:
1. **`templates/learning/learning_map_modern_style.html`** — основной шаблон (8133+ строк)
2. **`routes/learning_map_routes.py`** — маршруты (2848+ строк)
3. **`routes/archive_api.py`** — API архива (384 строки)
4. **`static/css/pages/learning_map.css`** — стили (если используется)

### Зависимости:
- Flask, Flask-Login
- SQLAlchemy
- Alpine.js (CDN)
- Bootstrap 5 (CDN)
- Bootstrap Icons (CDN)
- Lottie Player (CDN)

### Тестирование:
- Параметр `?reset_tour=1` — для сброса тура
- Параметр `?tab=<tab_name>` — для прямой навигации к вкладке
- Локальное логирование тура (только на localhost)

---

## 🎯 ЗАКЛЮЧЕНИЕ

Карта обучения — это **центральный хаб платформы MENTORA**, объединяющий:
- ✅ 7 основных вкладок с разным функционалом
- ✅ Интеграцию с диагностикой (IRT)
- ✅ Систему планирования обучения
- ✅ Отслеживание прогресса и статистики
- ✅ Архив завершенных активностей
- ✅ Мультиязычную поддержку
- ✅ Адаптивный дизайн

**Все модули работают независимо, но объединены единым интерфейсом и системой отслеживания прогресса.**

---

**Конец отчета**

