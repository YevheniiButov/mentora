# Настройка Telegram уведомлений

Система мониторинга поддерживает отправку уведомлений в Telegram для критических событий и новых регистраций.

## Шаг 1: Создание Telegram бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и создайте бота
4. Сохраните **токен бота** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Получение Chat ID

### Вариант 1: Через API (рекомендуется)

1. Найдите вашего бота в Telegram (по имени, которое вы дали при создании)
2. **ВАЖНО:** Отправьте боту любое сообщение (например, `/start` или просто "Привет")
3. Подождите несколько секунд
4. Откройте в браузере: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
   - Замените `<ВАШ_ТОКЕН>` на токен, который вы получили от BotFather
   - Пример: `https://api.telegram.org/bot123456789:ABCdefGHIjklMNOpqrsTUVwxyz/getUpdates`
5. Если видите ошибку `{"ok":false,"error_code":404,"description":"Not Found"}`:
   - Проверьте, что токен скопирован полностью (без пробелов)
   - Убедитесь, что вы отправили сообщение боту ПЕРЕД открытием ссылки
   - Попробуйте отправить еще одно сообщение боту и обновить страницу
6. Если видите `{"ok":true,"result":[]}` (пустой массив):
   - Это означает, что бот еще не получил сообщений или они уже обработаны
   - **Решение:** Отправьте боту новое сообщение (например, "Привет" или `/start`)
   - Сразу после отправки обновите страницу в браузере (или откройте ссылку заново)
   - Или используйте параметр `?offset=-1` в конце URL: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates?offset=-1`
7. Если все правильно, вы увидите JSON с данными в `result`. Найдите в ответе `"chat":{"id":123456789}` - это ваш Chat ID
   - Chat ID может быть отрицательным числом (для групп) или положительным (для личных чатов)
   - Пример правильного ответа: `{"ok":true,"result":[{"update_id":123,"message":{"chat":{"id":123456789}...}}]}`

### Вариант 2: Через другого бота (проще)

1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте ему команду `/start`
3. Бот сразу покажет ваш Chat ID

### Вариант 3: Через группу

Если хотите получать уведомления в группе:
1. Создайте группу в Telegram
2. Добавьте вашего бота в группу
3. Отправьте любое сообщение в группу
4. Используйте API: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
5. Найдите `"chat":{"id":-123456789}` (отрицательное число для групп)

## Шаг 3: Настройка переменных окружения

Добавьте в ваш `.env` файл или переменные окружения на сервере:

```bash
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

## Шаг 4: Перезапуск приложения

После добавления переменных окружения перезапустите приложение.

## Что будет отправляться в Telegram?

- 🔴 **Критические ошибки** - все ошибки уровня `critical` и `error`
- 🆕 **Новые регистрации** - уведомления о регистрации новых пользователей
- 🟢 **Входы пользователей** - уведомления о входе обычных пользователей (не админов)

## Отключение уведомлений

Если вы не хотите получать Telegram уведомления, просто не указывайте переменные окружения `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`. Система будет работать без Telegram, но продолжит отправлять email уведомления.

## Тестирование

После настройки вы можете протестировать отправку уведомлений, создав тестовую ошибку или зарегистрировав нового пользователя.

