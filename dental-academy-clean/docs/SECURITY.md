# 🔒 Security Features

## Реализованные меры безопасности

### 1. **Блокировка подозрительных запросов**
- Автоматическое обнаружение паттернов сканирования уязвимостей
- Блокировка IP после 10 подозрительных запросов
- Временная блокировка на 1 час
- Логирование всех подозрительных запросов

### 2. **Security Headers**
Добавлены следующие заголовки безопасности:
- `X-Content-Type-Options: nosniff` - предотвращает MIME-sniffing
- `X-Frame-Options: DENY` - защита от clickjacking
- `X-XSS-Protection: 1; mode=block` - защита от XSS
- `Strict-Transport-Security` - принудительное использование HTTPS
- `Content-Security-Policy` - политика безопасности контента
- `Referrer-Policy` - контроль передачи referrer
- `Permissions-Policy` - ограничение доступа к API браузера

### 3. **Rate Limiting**
- Глобальный лимит: 200 запросов в минуту на IP
- Дополнительный лимит: 1000 запросов в час на IP
- Используется Flask-Limiter для управления

### 4. **Защита от атак**
- SQL Injection - блокировка паттернов SQL
- XSS - блокировка JavaScript инъекций
- Path Traversal - блокировка доступа к системным файлам
- WordPress scanning - блокировка попыток сканирования WP
- PHP file scanning - блокировка поиска PHP файлов

### 5. **Мониторинг и логирование**
- Все подозрительные запросы логируются
- Админ-панель для просмотра заблокированных IP
- Возможность ручной разблокировки IP

## Использование

### Просмотр заблокированных IP
1. Зайдите в админ-панель
2. Перейдите в "Административные инструменты" → "Заблокированные IP"
3. Просмотрите список заблокированных адресов
4. При необходимости разблокируйте IP вручную

### Настройка
Файл `utils/security.py` содержит настройки:
- `BLOCK_THRESHOLD = 10` - количество запросов для блокировки
- `BLOCK_DURATION = 3600` - длительность блокировки (секунды)
- `SUSPICIOUS_PATTERNS` - список паттернов для блокировки

## Рекомендации

1. **Регулярно проверяйте логи** на наличие подозрительной активности
2. **Мониторьте заблокированные IP** в админ-панели
3. **Настройте уведомления** о критических событиях безопасности
4. **Используйте HTTPS** для всех соединений
5. **Регулярно обновляйте зависимости** для исправления уязвимостей


