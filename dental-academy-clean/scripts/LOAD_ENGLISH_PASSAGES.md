# 📚 Как загрузить свои English Passages

## Формат данных

Каждый passage должен иметь следующую структуру:

```python
{
    'title': 'Название текста',
    'content': '''
    Текст для чтения.
    Может быть многострочным.
    ''',
    'questions': [
        {
            'question_number': 1,
            'question_text': 'Текст вопроса?',
            'question_type': 'multiple_choice',
            'options': [
                'Вариант ответа A',
                'Вариант ответа B',
                'Вариант ответа C',
                'Вариант ответа D'
            ],
            'correct_answer': 0,  # Индекс правильного ответа (0 = первый вариант)
            'explanation': 'Объяснение почему это правильный ответ'
        },
        # ... больше вопросов
    ]
}
```

## Пример использования

1. Откройте файл: `scripts/seed_english_passages.py`

2. Найдите массив `passages_data` и замените примеры на свои:

```python
passages_data = [
    {
        'title': 'Ваш заголовок',
        'content': 'Ваш текст...',
        'questions': [
            {
                'question_number': 1,
                'question_text': 'Ваш вопрос?',
                'question_type': 'multiple_choice',
                'options': ['A', 'B', 'C', 'D'],
                'correct_answer': 0,  # 0=A, 1=B, 2=C, 3=D
                'explanation': 'Объяснение'
            }
        ]
    }
]
```

3. Запустите скрипт:

```bash
python3 scripts/seed_english_passages.py
```

## Важные моменты

- `correct_answer` - это **индекс** (0, 1, 2, 3), а не буква
- `question_type` должен быть `'multiple_choice'` для вопросов с вариантами
- Можно добавить любое количество вопросов к каждому passage
- Скрипт автоматически преобразует индексы в буквы (A, B, C, D)

