# 🔄 Обновление Enhanced Virtual Patient сценариев

## 📋 Что делает этот скрипт

Скрипт `update_enhanced_vp_scenarios.py` автоматически:
1. ✅ Находит старые сценарии (по частичному совпадению title)
2. ✅ Загружает 4 новых Enhanced версии:
   - **Complex Dental Problem** → `complex_problem_complete.json`
   - **Slaapproblemen** → `slaapproblemen_enhanced.json`
   - **Acute Chest Pain STEMI** → `chest_pain_stemi_enhanced.json`
   - **Chronische Rugpijn** → `chronic_back_pain_enhanced.json`
3. ✅ Удаляет старые версии из БД

---

## 🚀 Использование после деплоя

### **Вариант 1: Локально (перед деплоем)**

```bash
# 1. Убедитесь, что все Enhanced файлы на месте
ls cards/virtual_patient/*enhanced*.json
ls cards/virtual_patient/complex_problem_complete.json

# 2. Активируйте виртуальное окружение (если используется)
source venv/bin/activate  # или .venv/bin/activate

# 3. Запустите скрипт
python scripts/update_enhanced_vp_scenarios.py

# 4. Скрипт покажет:
#    - Текущие сценарии в БД
#    - Спросит подтверждение
#    - Выполнит обновление
#    - Покажет финальное состояние
```

---

### **Вариант 2: На продакшене (Render/сервер)**

#### **Через SSH (если доступен):**

```bash
# 1. Подключитесь к серверу
ssh your-server

# 2. Перейдите в директорию проекта
cd /path/to/dental-academy-clean

# 3. Активируйте окружение (если используется)
source venv/bin/activate

# 4. Запустите скрипт
python scripts/update_enhanced_vp_scenarios.py
```

#### **Через Render Shell:**

```bash
# 1. В Render Dashboard → ваш сервис → Shell
# 2. Выполните команды:

cd /opt/render/project/src  # или путь к вашему проекту
python scripts/update_enhanced_vp_scenarios.py
```

#### **Через Flask CLI (если настроен):**

```bash
# Если у вас есть Flask CLI команды
flask update-enhanced-vp
```

---

## ⚠️ ВАЖНО: Резервное копирование

**Перед запуском скрипта сделайте резервную копию БД:**

```bash
# PostgreSQL (например, на Render)
pg_dump $DATABASE_URL > backup_before_enhanced_update.sql

# Или через Render Dashboard:
# Database → Backup → Create Backup
```

---

## 📊 Что будет показано

Скрипт покажет:

```
🔍 Проверка текущего состояния БД...
📋 Текущие сценарии в БД:
   ID   1 | Complex Probleem                | dentistry           | Попыток:   5
   ID   3 | Slaapproblemen                  | general_medicine    | Попыток:   2
   ...

❓ Продолжить обновление? (yes/no): yes

📦 Обработка: cards/virtual_patient/complex_problem_complete.json
   📝 Новый title: 🎯 Complete Enhanced VP: Acute Pijn en Complexe Keuzes
   🏥 Specialty: dentistry
   ⭐ Difficulty: advanced
   🗑️  Найдено 1 старых сценариев для замены:
      - ID 1: 'Complex Probleem' (dentistry)
         ⚠️  ВНИМАНИЕ: У сценария 5 попыток...
   ✅ Создан новый Enhanced сценарий (ID: 15)
   💾 Изменения сохранены в БД

✅ ОБНОВЛЕНИЕ ЗАВЕРШЕНО
   📊 Обновлено сценариев: 4
   🗑️  Удалено старых: 4
```

---

## 🔍 Проверка после обновления

После выполнения скрипта проверьте:

1. **В браузере:**
   ```
   https://your-domain.com/virtual-patients
   ```
   Должны быть видны новые Enhanced сценарии с эмодзи в названиях.

2. **В админ панели:**
   ```
   https://your-domain.com/admin/virtual-patients
   ```
   Проверьте, что старые сценарии удалены, новые добавлены.

3. **В БД (через Flask shell):**
   ```python
   from app import app, db
   from models import VirtualPatientScenario
   
   with app.app_context():
       scenarios = VirtualPatientScenario.query.all()
       for s in scenarios:
           print(f"{s.id}: {s.title} ({s.specialty})")
   ```

---

## ❓ Частые вопросы

### **Q: Что произойдет с попытками (attempts) пользователей?**

**A:** 
- Попытки останутся в БД (связь через `scenario_id`)
- НО: старые сценарии будут удалены, поэтому ссылки на них будут битыми
- Это нормально: пользователи начнут новые попытки с Enhanced версиями
- Если нужно сохранить историю — используйте более сложный скрипт с миграцией attempts

### **Q: Можно ли откатить изменения?**

**A:** 
- Если есть резервная копия БД — да, восстановите из backup
- Иначе — нет, изменения необратимы

### **Q: Что если Enhanced версия уже существует?**

**A:** 
- Скрипт пропустит её (не будет дублировать)
- Покажет предупреждение: `⚠️ Enhanced версия уже существует в БД`

### **Q: Можно ли запустить скрипт несколько раз?**

**A:** 
- Да, скрипт идемпотентен
- Если Enhanced версии уже есть — пропустит
- Если старых нет — просто добавит новые

---

## 🐛 Если что-то пошло не так

1. **Проверьте логи:**
   ```bash
   # В Render Dashboard → Logs
   # Ищите ошибки с "update_enhanced_vp"
   ```

2. **Проверьте файлы:**
   ```bash
   ls -la cards/virtual_patient/*enhanced*.json
   ls -la cards/virtual_patient/complex_problem_complete.json
   ```

3. **Запустите скрипт в debug режиме:**
   ```python
   python -i scripts/update_enhanced_vp_scenarios.py
   # Затем в Python shell:
   >>> list_current_scenarios()
   >>> update_scenarios()
   ```

4. **Восстановите из backup** (если нужно)

---

## 📝 Сопоставление старых → новых сценариев

| Старый Title (Pattern) | Новый Enhanced файл |
|------------------------|---------------------|
| "Complex Probleem" | `complex_problem_complete.json` |
| "Slaapproblemen" | `slaapproblemen_enhanced.json` |
| "Acute Pijn op de Borst" / "STEMI" | `chest_pain_stemi_enhanced.json` |
| "Chronische Rugpijn" / "Rugpijn" | `chronic_back_pain_enhanced.json` |

---

## ✅ Чек-лист перед деплоем

- [ ] Все 4 Enhanced JSON файла на месте
- [ ] Скрипт протестирован локально
- [ ] Резервная копия БД создана
- [ ] Понимаете последствия (старые сценарии удалятся)
- [ ] Готовы к запуску после деплоя

---

**Готово! 🚀 После деплоя просто запустите скрипт и следуйте инструкциям.**


