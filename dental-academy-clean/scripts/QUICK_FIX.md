# 🚨 БЫСТРОЕ ИСПРАВЛЕНИЕ ПРОДАКШЕНА

## Проблема
- Learning Map не загружается (500 ошибки)
- Отсутствуют колонки в базе данных

## Решение (2 минуты)

### 1. Зайти в Render Shell
- Откройте Render Dashboard
- Выберите ваш web service
- Нажмите "Shell"

### 2. Запустить скрипт
```bash
cd /opt/render/project/src
./scripts/run_production_fix.sh
```

### 3. Готово! ✅
- Скрипт автоматически исправит все проблемы
- Покажет статус выполнения
- Learning Map заработает

## Что делает скрипт:
1. ✅ Проверяет отсутствующие колонки
2. ✅ Добавляет все нужные колонки
3. ✅ Проверяет результат
4. ✅ Показывает статус

## Если что-то пошло не так:
```bash
# Проверить статус
psql $DATABASE_URL -c "SELECT column_name FROM information_schema.columns WHERE table_name = 'personal_learning_plan' AND column_name = 'current_category_focus';"
```

**Время выполнения: 30 секунд** ⏱️
