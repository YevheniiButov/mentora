# Исправление: План обучения не создаётся после диагностики

**Дата:** 2025-10-26  
**Проблема:** План обучения не создаётся после прохождения диагностики на продакшене

## Проблема

Пользователь прошёл диагностику, ответил на 9 вопросов правильно (30%), но план обучения не появился.

## Причина

В файле `routes/diagnostic_routes.py` (строка 1135) проверка типов сессий была неполной:

```python
if diagnostic_session.session_type in ['diagnostic', 'express', 'preliminary', 'readiness']:
```

**НО** при создании сессии (строка 180) используется:
```python
diagnostic_session = DiagnosticSession.create_session(
    user_id=current_user.id,
    session_type='adaptive_diagnostic',  # ← Этот тип не входит в список!
    ip_address=request.remote_addr
)
```

В результате план обучения не создаётся для новых типов диагностики.

## Исправление

Добавлены все недостающие типы в проверку:

```python
if diagnostic_session.session_type in [
    'diagnostic', 
    'express', 
    'preliminary', 
    'readiness', 
    'adaptive_diagnostic',  # ← ДОБАВЛЕНО
    'full',                 # ← ДОБАВЛЕНО
    'learning',             # ← ДОБАВЛЕНО
    'quick_30',             # ← ДОБАВЛЕНО
    'full_60'               # ← ДОБАВЛЕНО
]:
```

## Файл

- `routes/diagnostic_routes.py`, строка 1135

## Тестирование

После деплоя на продакшен:
1. Пройти диагностику (любой тип)
2. Проверить, что план обучения создаётся
3. Проверить логи: должно появиться сообщение "🔍 ОТЛАДКА: Новый план создан: {id}"

## Превентивные меры

Рекомендуется:
1. Создать enum для session_type
2. Использовать этот enum при создании сессий
3. Убрать хардкод списков типов

