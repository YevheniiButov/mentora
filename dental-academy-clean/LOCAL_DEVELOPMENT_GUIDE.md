# 🚀 Руководство по локальной разработке

## Проблема
При локальной разработке приложение перенаправляет на продакшн URL `https://bigmentor.nl` вместо локального `http://127.0.0.1:5002`.

## Решение

### Вариант 1: Использовать готовый скрипт (рекомендуется)
```bash
python3 run_local.py
```

### Вариант 2: Установить переменные окружения вручную
```bash
export BASE_URL=http://127.0.0.1:5002
export SERVER_NAME=127.0.0.1:5002
python3 app.py
```

### Вариант 3: Изменить файл .env (если доступен)
```bash
# В файле .env изменить:
BASE_URL=http://127.0.0.1:5002
```

## Проверка
После запуска откройте браузер: http://127.0.0.1:5002

Карта обучения теперь должна работать по локальному адресу:
http://127.0.0.1:5002/en/learning-map/

## Что было исправлено
1. ✅ Исправлена ошибка 404 для маршрута learning-map
2. ✅ Добавлены правильные настройки URL для разработки в `config.py`
3. ✅ Создан скрипт `run_local.py` для удобного запуска
4. ✅ URL'ы теперь генерируются для локального сервера

## Технические детали
- `BASE_URL`: Базовый URL для генерации ссылок
- `SERVER_NAME`: Имя сервера для Flask
- Эти настройки переопределяют продакшн конфигурацию при локальной разработке
